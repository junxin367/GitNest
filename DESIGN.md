# Web UI 与交互设计规范

## 1. 适用范围与设计原则

本规范适用于桌面端 Web 产品的页面设计、前端实现和代码审查，用于统一高频视觉与交互行为。它不是完整的 Design System，也不替代具体业务需求。

本文件只维护可跨页面、跨场景或在同类组件中复用的通用设计、视觉样式、交互行为与可操作性规范。具体业务规则、单个页面方案、单次缺陷记录、临时修复措施、技术选型和实现细节不得写入本文件，应记录在对应需求、设计或技术文档中。

1. **一致性优先。** 同类页面、组件和交互必须复用统一规则，禁止为局部页面创建无必要的独立样式。
2. **清晰优先。** 页面应明确表达信息层级、当前状态、可执行操作和操作结果。
3. **效率优先。** 高频操作应直接可见，低频操作可以收纳，但不得隐藏关键操作。
4. **反馈及时。** 用户触发操作后必须立即获得视觉反馈；异步操作必须显示进行状态。
5. **设计变量优先。** 颜色、字号、间距、圆角、阴影、动效和层级必须使用统一变量，禁止在业务代码中随意新增近似值。
6. **默认遵循本规范。** 只有存在明确业务原因时才可以偏离，并应在设计或代码中说明原因。

## 2. 设计基础

### 2.1 推荐基础变量

| 类型 | 推荐默认值 |
|---|---|
| 字体 | 系统无衬线字体栈 |
| 正文字号 | `14px` |
| 辅助文字 | `12px` |
| 强调正文 | `16px` |
| 页面标题 | `24px` |
| 区块标题 | `18px` |
| 常规行高 | `1.5` |
| 标题行高 | `1.3` |
| 常规字重 | `400` |
| 强调字重 | `500` 或 `600` |
| 间距体系 | `4 / 8 / 12 / 16 / 24 / 32 / 48px` |
| 常规圆角 | `8px` |
| 小圆角 | `4px` |
| 大圆角 | `12px` |
| 边框 | `1px solid var(--color-border)` |
| 默认组件高度 | `35px` |
| 紧凑组件高度 | `32px` |
| 大型组件高度 | `40px` |
| 图标尺寸 | `16 / 20 / 24px` |
| 页面最大宽度 | `1440px` |
| 页面安全边距 | 普通页面统一 `12px`；贴边型特殊页面可显式覆盖为 `0` |
| 微交互动效 | `150～200ms` |
| 浮层与位移动效 | `200～300ms` |

7. 全局字体、字号、字重和行高必须统一使用基础变量；相同层级的标题、正文和辅助文字必须保持一致。
8. 间距必须从统一间距体系中选择，禁止通过任意像素值反复修补布局。
9. 同一页面不得同时出现多套按钮高度、输入框高度、圆角或区块间距。
10. 边框用于表达边界和状态，不得与阴影或多个状态描边重复堆叠形成过重视觉效果；状态变化不得因边框粗细改变导致组件尺寸跳动。
11. 阴影应主要用于浮层、悬浮组件和需要脱离页面层级的内容；普通卡片优先使用背景或边框分隔。
12. 图标必须使用统一风格和线条粗细，同一操作在不同页面不得使用含义不同的图标。

### 2.2 语义颜色

颜色必须通过语义变量使用，至少包括：

| 变量类型 | 用途 |
|---|---|
| `--color-primary` | 主要操作、选中状态、关键强调 |
| `--color-success` | 成功状态 |
| `--color-warning` | 警告和风险提示 |
| `--color-danger` | 错误、删除和高风险操作 |
| `--color-info` | 普通信息提示 |
| `--color-text-primary` | 主要文字 |
| `--color-text-secondary` | 辅助文字 |
| `--color-text-disabled` | 禁用文字 |
| `--color-bg-page` | 页面背景 |
| `--color-bg-surface` | 卡片、弹窗和内容容器背景 |
| `--color-border` | 常规边框 |
| `--color-focus` | 键盘焦点描边 |
| `--color-overlay` | 遮罩层 |

13. 信息状态不得只通过颜色表达，必须同时使用文字、图标、形状或位置进行区分。
14. 文本与背景的常规对比度应至少达到 `4.5:1`；大号文字和非文本关键控件应至少达到 `3:1`。

## 3. 页面布局与排版

15. 页面应采用稳定的主布局，由头部、导航或侧边栏、主内容区和必要的底部区域组成。
16. 同类页面的标题区、说明区、筛选区、内容区和操作区必须保持相同顺序和间距。
17. 页面标题应位于内容区顶部；主要操作应位于标题区右侧或紧邻标题下方，不得散落在页面不同位置。
18. 面包屑只用于表达多层级位置，不得代替页面标题或主要导航。
    - 二级及更深页面必须在主内容开始前展示面包屑，明确当前页面所属的一级入口和当前层级。
    - 父级面包屑必须可以点击，并提供稳定返回入口；当前页面项不可点击。
    - 不得只依赖浏览器后退作为返回方式，因为用户可能通过刷新、收藏或分享链接直接进入详情页。
19. 侧边栏应使用统一宽度，建议展开宽度为 `240px` 左右、收起宽度为 `64px` 左右；收起后必须保留可识别图标和 Tooltip。
20. 页面内容宽度应受最大宽度和安全边距约束；超宽内容不得无限拉伸。
    - 应用最小画布尺寸只能在全局布局壳层统一定义。页面内容区默认使用 `width: 100%`，并通过 Flex、Grid、百分比或弹性列分配可用空间；禁止在业务页面中用多组固定像素宽度拼接或模拟页面最小宽度。
    - 浏览器缩放和系统显示缩放不得导致达到约定最小物理画布尺寸时错误出现横向滚动。需要固定最小画布时，应由全局布局根据设备像素比统一换算，业务页面不得自行判断视口断点。
21. 页面、卡片和区块之间必须使用统一间距；相关内容应更靠近，不相关内容应通过更大间距或分隔线区分。
22. 固定头部、底部操作栏和侧边栏必须明确固定区域与滚动区域，禁止产生双重滚动或遮挡内容。
23. 固定定位元素必须避让浏览器安全区域、滚动条、底部操作区和其他重要交互区域。
24. 内容过长时应由实际溢出的内容区域滚动，不得通过压缩字号、组件高度或行高强行容纳。滚动范围必须明确且可操作，避免页面、浮层和内部区域形成无必要的嵌套滚动或无法操作的伪滚动区域。
25. 表单标签必须使用统一布局。纵向表单默认标签位于字段上方；横向表单必须统一标签宽度和对齐方式。
25.1. **筛选工具栏应按整体使用流程布局。** 工具栏整体默认左对齐，其中的搜索、筛选条件和操作按使用顺序从左向右排列。
25.2. **筛选提交操作应位于条件之后。** 采用显式提交的筛选工具栏，搜索框和各筛选项应先排列，执行搜索、查询或筛选的提交按钮默认紧随全部条件，作为筛选操作组中的最后一个主要操作；存在重置或清空按钮时，次要操作应放在主要提交按钮之前。采用即时生效筛选时可以不显示提交按钮，但筛选条件的排列顺序必须保持稳定。

### 3.1 布局稳定性

26. 页面初次加载、异步刷新和功能交互过程中，非用户主动触发的内容不得导致已显示元素发生明显位移。
27. 图片、视频、图表和异步组件必须预先声明宽高、宽高比或最小高度，禁止资源加载完成后再撑开页面。
28. 骨架屏、Loading 占位和最终内容必须保持接近的尺寸与结构，不得使用与最终内容尺寸明显不一致的通用占位块。
29. 表格、列表和卡片刷新时应保留容器尺寸；禁止先清空内容再显示 Loading，导致下方区域上下移动。
30. 按钮进入 Loading、状态文字变化或图标出现时必须保持原有尺寸，禁止因内容增减改变周边布局。
31. 校验错误、帮助信息和状态提示应使用预留区域或稳定的信息容器，避免同组表单反复上下跳动。
32. 展开、收起、筛选和标签切换等用户主动触发的布局变化必须具有明确来源和连续过渡，并保持用户当前视线位置。
33. 动画优先使用 `transform` 和 `opacity`；禁止通过频繁修改 `width`、`height`、`margin`、`padding` 或定位值制造无必要的布局重排。
34. 异步内容更新不得自动改变滚动位置；插入新内容时应保持用户当前阅读位置，除非用户明确执行跳转或定位操作。
35. 字体和图标资源必须提供尺寸接近的回退方案，避免加载前后因字宽、行高或图标尺寸变化造成布局偏移。
36. 页面滚动条出现、消失或被锁定时不得造成整体横向位移；弹窗锁定背景滚动时必须补偿滚动条宽度。
37. 固定头部、吸顶区域、侧边栏和底部操作栏的尺寸必须稳定；状态变化不得覆盖内容或改变主内容区起始位置。
38. 浮层、Tooltip、下拉菜单和 Toast 必须脱离普通文档流显示，不得撑开或挤压页面内容。
39. 无法避免的内容尺寸变化必须限制在当前操作区域内部，不得连带移动页面中的无关区域。

## 4. 组件通用规范

39.1. **组件布局应匹配信息结构。** 组件具有并列、对照或关联内容时，应优先通过分区或横向布局表达关系，避免因统一套用单一结构而形成明显窄高、拥挤或无意义的纵向堆叠。
39.2. **复杂组件应按实际需要设计扩展能力。** 当组件功能较多、使用场景差异明显且扩展需求稳定时，可通过配置项、组合组件或插槽提供不同能力；功能简单或场景单一时，不得为了潜在复用进行过度设计。
39.3. **紧凑型筛选与操作行应限制组件尺寸。** 日期选择器、下拉框、输入框和按钮默认最大宽度为 `300px`、最大高度为 `35px`；控件应按内容和场景使用必要宽度，不得无意义拉伸。存在明确内容展示或业务操作需要时可以放宽。
39.4. **同一行内的组件应保持高度一致。** 输入与操作组件必须使用一致高度。
39.5. **带边框组件嵌套时应保留层级间距。** 带边框组件嵌套在另一个带边框的卡片、容器或区块中时，内外边框之间必须保留统一间距，默认使用 `12px`；不得让两层边框直接贴合。
39.6. **占满容器的组件应使用弹性尺寸。** 组件及其内部区域应优先通过百分比、Flex、Grid 或其他弹性方式分配可用宽度，不得通过多个固定像素宽度相加决定组件或上级页面的总宽度。
39.7. **跨页面引导入口必须保持布局稳定。** 依赖异步状态决定是否显示的引导、提醒或恢复入口，应占用固定位置并在加载时使用尺寸接近的占位；不得因状态返回而推动导航或无关内容位移。
39.8. **跨页面引导不得复制业务表单。** 引导只说明当前状态、未满足条件和下一步，并提供前往权威业务页面的稳定返回路径；不得在多个页面维护同一业务配置的独立表单和状态逻辑。

### 4.1 通用状态

40. 所有可交互组件必须定义并区分 `Default`、`Hover`、`Active`、`Focus`、`Disabled`、`Loading` 和 `Error` 状态；可选择组件还必须区分 `Selected` 状态。
41. Hover、Active、Focus、Selected 和 Disabled 必须能够清晰区分。多个状态同时存在时必须定义视觉优先级；Selected、Disabled 等持续状态不得被 Hover 等临时状态覆盖，相同语义的状态应保持一致的视觉表达。
42. 键盘聚焦时必须保留清晰、连续的 Focus 描边，不得仅因视觉偏好移除 `outline`。常规控件默认使用 `1px` 焦点描边，不得使用过粗描边压缩内容或形成突兀的双层边框；只有高风险操作或特殊无障碍场景可以明确提高描边强度。
43. 按钮、链接、下拉触发器、可点击卡片和其他具有独立操作语义且处于可用状态的元素悬停时必须使用 `pointer`；下拉菜单、选择器、组合框和命令菜单中的可用选项同样必须显示手型光标。表单和筛选字段的说明性标签默认使用普通箭头光标；只有标签自身直接承担复选、单选、开关切换或其他独立操作时才使用 `pointer`。
44. 禁用元素应使用 `not-allowed` 或默认光标，并通过降低对比度、移除阴影或取消交互反馈明确表达不可用状态。
45. 可点击性不得只依靠光标表达，必须同时通过按钮形态、链接样式、边框、背景或悬停反馈体现。
46. 禁用状态不得响应点击、键盘提交或悬停操作；存在原因时应提供说明。
46.1. **空状态、禁用状态和阻断原因必须来源明确。** 原因文案应基于已验证的业务状态，说明未满足条件、影响和恢复动作；不得仅根据页面位置、默认假设或前端临时状态推断原因。

### 4.2 按钮与输入组件

47. 一个操作区域通常只应有一个主要按钮；次要操作使用次按钮、文字按钮或更多操作入口。
48. 危险操作必须使用危险语义样式，并在可能造成不可逆影响时增加确认步骤。
49. 按钮进入 Loading 后必须保留原有宽度、阻止重复提交，并显示明确的处理中状态。
50. 输入框、下拉列表和日期选择器必须统一高度、边框、圆角、内边距和状态样式。浏览器原生控件无法满足项目主题、布局或交互规范时，应使用项目公共组件统一封装。
50.1. 预计只需输入短结构化内容且无需换行的字段应使用单行输入框；预计可能包含完整语句、多项信息或需要保留换行的字段应使用多行文本组件。组件类型必须根据预期内容长度和信息结构选择，不得仅根据字段名称固定选择，也不得用单行输入框压缩长内容。
51. 表单字段必须具有可识别标签；占位符只能提供示例或提示，不得代替字段标签。
52. 字段校验错误应显示在字段附近，并说明问题及修正方式；不得只改变边框颜色。
53. 复选框用于多选或独立开关式确认，不得用多个复选框模拟互斥单选。
54. 下拉浮层必须设置与视口可用空间共同生效的最大高度，不得随选项数量无限增长；选项超出最大高度后使用内部滚动。可选项较多、名称相似或查找成本较高时，必须使用支持搜索的选择组件；仅因视口可用空间不足而产生滚动，不单独构成必须支持搜索的条件。已选值、当前高亮项和禁用项必须能够区分。
55. 日期选择器必须明确日期格式、可选范围和时区；日期范围不得依赖用户猜测开始与结束顺序。
    - 单月日历面板高度不得超过宽度 `50px` 以上。
    - 日期格不得使用正方形，应宽大于高，且垂直间距小于水平间距。

### 4.3 图标、提示与分页

56. 图标按钮必须提供可访问名称，并通过 Tooltip 或可见文字说明其含义。
57. Tooltip 只用于补充简短说明，不得承载关键流程、错误信息或必须阅读的内容。鼠标触发的解释、介绍类 Tooltip 与 Hover Card 应使用统一的悬停意图延迟，默认在稳定悬停 `500ms` 后出现，特殊场景可延长至 `1000ms`；键盘聚焦时应及时显示。浮层内容可交互或需要指针移入阅读时，必须允许指针进入浮层，并在悬停、聚焦或用户主动关闭前保持可见。
58. Tooltip 和下拉浮层不得被父容器裁剪；位置不足时应自动翻转或调整。
59. Toast 用于反馈全局操作结果，不得用于字段校验或需要长期保留的关键错误；全局 Toast 统一从页面顶部居中出现。
60. 分页必须显示当前页状态；数据量较大时应提供总数、每页数量或明确的加载更多机制。
61. 列表和可点击卡片必须提供明确的悬停、选中和键盘焦点状态；卡片内部存在多个操作时，不得让整张卡片产生歧义点击。

## 5. 数据展示规范

### 5.1 数据表格

62. 同一表格中的表头文字必须统一左对齐，不得跟随单元格内容单独居中或右对齐。单元格内容默认左对齐，包括普通数字、计数、状态、操作和其他组件化内容；只有金额、价格、百分比等需要逐行纵向比较的指标，或存在明确业务要求时，才可以显式设置为右对齐。不得仅因字段类型或徽标样式改变默认对齐方式。
63. 同一产品中的表头高度、数据行高度、边框方式和操作列位置必须统一。
    - 表格应根据列内容特征合理分配列宽，并在占满内容区时使列宽总和适配 `100%` 容器。标识、日期、版本、状态和普通数值等短内容列应优先保持单行；长文本或实际空间不足时，再根据内容语义选择换行、截断或横向滚动。
64. 操作列应固定在表格末端；主要行操作直接显示，低频操作可以收入更多菜单。
65. 单元格内容不得越过当前列边界或覆盖相邻列。空间不足时必须根据内容语义选择换行或截断隐藏：说明、原因等可阅读长文本优先换行；技术标识、日期、状态等单行内容优先截断，并允许通过 Tooltip、展开区域或详情页面查看完整内容。
66. 表格过宽时必须使用横向滚动；关键标识列和操作列可以固定，禁止压缩到无法阅读。
67. 表格必须分别设计 Loading、Empty、Error 和 No Search Results 状态，不得以空白表格代替。
68. 排序、筛选和分页状态必须可识别；刷新、返回或重新进入页面时应按业务需要保留有效条件。

### 5.2 图表

69. 图表必须标明数据名称、单位、时间范围和必要的数据来源或更新时间。
70. 折线图的时间轴必须按时间顺序排列，并避免使用误导性的截断坐标轴。
71. 多组数据不得只依靠颜色区分，应同时使用图例、线型、点型、标签或直接标注。
72. 图例名称必须与业务概念一致，不得使用内部字段名或含义不明的缩写。
73. 需要精确读取、核对或导出的数据，必须同时提供具体数值、明细表格或数据查看入口。
74. 图表在无数据、加载失败和数据不完整时必须显示明确状态，不得展示可能被误解的空坐标系。

## 6. 交互与状态反馈

75. 用户触发点击、提交、切换或拖拽后必须立即出现反馈，建议在 `100ms` 内显示按压、Loading 或状态变化。
76. 短时局部操作使用按钮 Loading；局部内容刷新使用区域 Loading；结构化内容首次加载优先使用骨架屏；长时任务应显示进度或阶段说明。
77. 成功、失败、警告和信息反馈必须使用统一的语义样式和图标。
78. 字段错误使用行内提示；全局操作结果以及不阻塞页面结构的数据读取失败使用 Toast；阻断关键流程的错误必须使用第 9 节定义的错误缺省状态。
80. 操作失败后应尽量保留用户输入、筛选条件和当前上下文，禁止无提示清空。
81. 删除、覆盖、离开未保存页面等高风险操作必须明确说明影响范围，不得使用含义模糊的确认文案。
82. 无法执行的操作应优先说明原因；不得让用户点击后才发现无权限、条件不满足或数据无效。

## 7. 弹窗、浮层与层级

### 7.1 层级体系

| 层级 | 推荐 `z-index` |
|---|---:|
| 页面内容 | `0` |
| 固定导航和固定操作区 | `100` |
| 下拉菜单和 Popover | `200` |
| Tooltip | `300` |
| 遮罩层 | `400` |
| Modal 和 Drawer | `500` |
| Toast | `600` |

83. 业务代码不得随意设置互相冲突的 `z-index`，新增层级必须纳入统一层级体系。
84. Toast 必须显示在普通 Modal 和 Drawer 上方。
85. 遮罩层必须使用统一颜色和透明度，推荐使用约 `40%～50%` 的深色遮罩。
86. Modal 打开后必须锁定背景滚动，并将键盘焦点限制在弹窗内部；关闭后焦点应返回触发元素。
87. 普通信息弹窗可以支持点击遮罩关闭；表单、危险操作或存在未保存内容的弹窗不得直接通过遮罩关闭。
88. 遮罩点击关闭必须以完整指针序列判断：只有指针按下和释放都发生在遮罩自身，且期间未进入弹窗内容区时才允许关闭；从弹窗内容区按下后移动到遮罩释放，或从遮罩按下后进入弹窗内容区，必须忽略该次关闭。不得仅依据 `mouseup`、`pointerup` 或事件冒泡结果关闭弹窗。
89. 存在未保存内容时，关闭 Modal、Drawer 或离开页面必须进行二次确认。
90. 应避免多层 Modal 嵌套；需要连续操作时优先使用步骤流程、抽屉或替换当前弹窗内容。
91. 弹窗内容过长时应保持标题和底部操作区可见，仅滚动内容区域。
92. 弹窗的主要按钮应位于统一位置，通常位于底部右侧；取消操作应与主要操作明确区分。

### 7.2 业务弹窗类型与结构

92.1. **业务弹窗默认分为信息弹窗和容器弹窗。** 存在明确业务或交互原因时可以采用其他形态，并应在设计中说明偏离原因。
    - 信息弹窗用于需要用户确认或明确知晓、会阻断当前流程的信息，包括普通确认、危险确认和放弃未保存内容；非阻断的全局操作结果应使用 Toast。信息弹窗的标题、说明、语义状态和操作区必须使用统一结构。
    - 信息弹窗的语义图标固定在左侧，标题和说明在右侧纵向排列；不得将图标、标题和说明上下堆叠占用额外高度。
    - 容器弹窗用于承载较大范围的展示或操作内容，默认通过标准弹窗或右侧抽屉两种形态表达；同类内容差异应优先通过统一结构和内容组合处理，不得新增语义和行为重复的弹窗类型。
    - 标准容器弹窗使用紧凑头部、独立滚动正文和可选固定底部操作区；业务内容不得自行重建遮罩、定位、头部或底部外壳。
    - 容器弹窗正文默认直接使用容器提供的内容区，不得再用覆盖整个正文的卡片边框、背景和圆角重复包裹；确有分组语义的局部内容可以使用边框。
    - 容器弹窗只有一至两个作用于整体内容的全局操作时，应优先放入固定底部操作区；存在较多独立操作、行级操作或操作与局部内容强关联时，可以保留在正文对应位置。
    - 底部操作区必须遵循按钮通用层级，不得将唯一主要操作显示为普通描边按钮。
    - 右侧抽屉必须遵循与标准弹窗一致的关闭、焦点管理、滚动和层级规则，主要差异体现在容器位置与响应式尺寸。
    - 右侧抽屉宽度应同时受视口比例和最大宽度约束，并根据可用空间调整内部信息栏数；空间充足时可增加并列信息，空间不足时应逐级减少栏数，不得使用固定窄宽度造成无必要的纵向堆叠。
    - 业务页面应优先复用项目统一的信息弹窗和容器弹窗结构，不得重复实现语义和行为相同的弹窗外壳。

## 8. 动效与过渡

93. Hover、颜色、边框和轻微缩放等微交互应使用 `150～200ms`。
94. 弹窗、抽屉、下拉浮层和位移动画应使用 `200～300ms`。
95. 进入动画优先使用 `ease-out`；退出动画应更快，避免阻碍下一步操作。
96. 页面内容切换、弹窗开闭和浮层出现必须具有自然过渡，禁止无反馈地生硬闪现。
97. 动画不得引起大面积布局跳动；优先使用 `opacity` 和 `transform`，避免频繁改变尺寸和位置计算。
98. 禁止使用影响操作效率的长时间动画、循环装饰动画或无业务意义的复杂转场。
99. 系统必须支持 `prefers-reduced-motion`，在用户选择减少动态效果时关闭或显著缩短非必要动画。

## 9. 加载、空状态与异常状态

100. 页面和主要数据组件必须至少覆盖 `Loading`、`Empty`、`Error`、`No Search Results`、`Disabled` 和 `Permission Denied` 状态。
101. 加载状态应预留稳定尺寸，避免加载前后内容区域大幅跳动。
102. 骨架屏结构应接近最终内容，不得使用与实际页面结构无关的占位块。

### 9.1 错误缺省状态

- 页面级阻断错误应保留页面框架、标题、导航和仍然有效的筛选条件，并使用错误缺省状态替换主内容区。
- 区块级阻断错误只替换发生失败的区块内容，其他相互独立且数据有效的区块继续展示和操作。
- 错误缺省状态应与空状态处于同一视觉层级，至少包含语义图标、明确标题和简短原因；操作入口根据实际可恢复能力提供，不强制必须包含重试。
- 失败区域的数据有效性无法确认时，不得继续展示指标占位、空表格、空列表或正常状态说明。
- 禁止在原有失效内容上方插入错误 Alert 后继续展示原内容。Alert 只用于需要与有效内容同时存在的警告、局部异常或数据不完整提示。

104. 空状态必须说明当前缺少什么，并提供用户可以执行的下一步操作。
105. 搜索无结果必须说明当前条件未匹配数据，并提供清除或修改筛选条件的入口；不得与业务尚无数据使用相同文案。
106. Permission Denied 必须说明权限不足，并提供返回、申请权限或联系管理员等可执行入口。
107. 禁止使用空白页面、空白卡片或空白表格表达加载、异常或无数据状态。

## 10. 响应式与可访问性

108. 页面以桌面端 Web 为主，同时必须适配常见较窄窗口。
109. 窄屏时应优先调整栏数、排列方向、导航形态和操作收纳方式，禁止简单缩小全部字号和控件。
110. 主要操作在窄屏下必须保持可见，或收纳到名称明确的更多操作入口。
111. 表格在窄屏下应横向滚动，不得压缩列宽、字号和行高至无法阅读。
112. 所有可交互元素必须支持键盘访问和清晰的焦点顺序；不得制造键盘无法进入或无法退出的区域。
113. 模态弹窗、菜单、下拉列表和日期选择器必须支持键盘关闭，通常使用 `Esc`。
114. 图标按钮、表单字段和自定义交互控件必须具有可被辅助技术识别的名称、状态和关联关系。
115. 点击或触摸目标建议不小于 `32 × 32px`，主要操作建议不小于 `40 × 40px`。

## 11. 设计与开发检查清单

- [ ] 是否使用统一的字体、字号、字重和行高？
- [ ] 是否只使用规定的间距、圆角、组件高度和图标尺寸？
- [ ] 同类页面的标题区、筛选区、内容区和操作区是否一致？
- [ ] 页面是否存在任意像素修补或重复的近似设计变量？
- [ ] 所有可点击元素是否具有明确视觉样式和 `pointer` 光标？
- [ ] Hover、Active、Focus、Selected、Disabled 是否能够清晰区分？
- [ ] 键盘 Focus 样式是否完整保留？
- [ ] 禁用和不可执行操作是否说明原因？
- [ ] 异步操作是否显示合适的 Loading，且按钮、提示、内容刷新和滚动条变化不会造成周边元素跳动？
- [ ] 页面和主要数据组件是否覆盖 Loading、Empty、Error 和 No Search Results，并为图片、图表、骨架屏和异步内容预留稳定尺寸？
- [ ] 阻断错误是否以错误缺省状态替换失败区域，且未与无法确认有效性的原内容同时展示？
- [ ] 操作失败后是否保留用户已输入内容和当前上下文？
- [ ] 表格文字、数字、金额和百分比是否按规则对齐？
- [ ] 长文本、宽表格和长内容是否具有明确的溢出处理？
- [ ] 图表是否标明单位、时间范围，并避免只用颜色区分？
- [ ] Modal 是否锁定背景滚动、管理焦点、处理未保存内容，并避免遮罩指针释放造成误关闭？
- [ ] `z-index`、遮罩透明度和浮层层级是否使用统一变量？
- [ ] 动效时长是否统一，且未造成布局跳动或操作延迟？
- [ ] 窄屏布局是否通过重排和收纳适配，而非简单缩小内容？
- [ ] 文本对比度、键盘操作和辅助说明是否满足可访问性要求？
